# TODO

- [ ] Spustit nový SharePoint sync/ingest, aby se do `sharepoint_sync_state.json` a Chroma metadat propsaly `sharepoint_url` odkazy i pro starší dokumenty.
- [ ] Ověřit ve web UI, že v panelu "Source files in Chroma (read-only)" vedou názvy dokumentů přímo na SharePoint URL.
